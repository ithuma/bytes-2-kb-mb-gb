# bytes-2-kb-mb-gb
Function that convert bytes to kilobytes megabytes and gigabytes v1.0 preparation to Rapidu.net Api/Json Reader

Function will help changes bytes to kilo mega giga bytes.
Yes, input is string ;) casue I read file size from rapidu .AsString so it's easier to convert

Inside txt file is easy example how to. Copy function to your lpi/pas file and call it.

Result is String

If you use it in your soft just remember to leave info about the suthor.

It's not perfect solution, but few lines of code make my life simple :)
